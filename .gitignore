# ---------- OS ----------
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ---------- Editors / IDEs ----------
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace

# ---------- Logs & temp ----------
*.log
*.tmp
*.bak

# ===================================================================
# Node / React (Vite)  —  frontend/
# ===================================================================
node_modules/
# Vite build output & caches
dist/
dist-ssr/
.vite/
.cache/
*.local

# Package-manager debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# Test / lint caches
coverage/
.eslintcache

# ---------- Environment / secrets ----------
# Ignore all env files (incl. Vite .env.local etc.) but keep the template
.env
.env.*
!.env.example
*.pem
*.key

# ===================================================================
# Laravel / PHP  (Phase 2+ — safe to keep now so it's ready later)
# ===================================================================
/vendor/
/storage/*.key
/storage/framework/cache/*
/storage/framework/sessions/*
/storage/framework/views/*
/storage/logs/*
/bootstrap/cache/*
/public/storage
/public/hot
Homestead.json
Homestead.yaml
auth.json

# ---------- Docker (Phase 2+) ----------
docker-compose.override.yml

# ---------- Misc ----------
.phpunit.result.cache
