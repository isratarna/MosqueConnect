*.sh text eol=lf

# These are all consumed inside Linux containers, where a stray CR breaks the
# file: Docker mis-parses continued RUN lines, and nginx/PHP read the CR as
# part of a directive's value. Windows checkouts must keep LF for them.
Dockerfile* text eol=lf
*.conf text eol=lf
*.ini text eol=lf
